name: secure_random
description: A secure random generator to generate cryptographically secure random string.
version: 1.1.0
homepage: https://github.com/mingchen/secure_random

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  test: ^1.3.0
